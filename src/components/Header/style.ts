import styled from 'styled-components';

export const Container = styled.header`
    background: linear-gradient(to right, #004056, #2C858D);
    
`;

export const Content = styled.div`
    max-width: 1120px;
    margin-left: 1rem;
    padding: 0.75rem 1rem 0.75rem;
    display:flex;

`;